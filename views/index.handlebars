<div class="container">
    <div class="jumbotron">
        <h1 class="display-4"> <i class="fas fa-newspaper"></i> Mongo Scraper</h1>
        <p class="lead">This is a simple app using Node, Express, MongoDB, and Handlebars.
        </p>
        <hr class="my-4">
        <p>These articles are scraped from Rising Stack website using Cheerio.</p>
        <p>You can save any articles that you like and access them later.</p>

    </div>
    <div class="row justify-content-center">
        <div class="col-md-12">
            <ul class="articles">
                {{#each articles}}
                <li>
                    <a href={{this.link}} data-id={{this._id}} target="_blank">
                        <h3>{{this.title}}</h3>
                    </a>
                    <br>
                    <p>{{this.content}}</p>

                    <button class="btn btn-primary save float-right"><i class="fas fa-plus-circle"></i></i> Save
                        Article</button>
                </li>
                {{/each}}
            </ul>

        </div>
    </div>
</div>