<div class="container">
    <div class="jumbotron">
        <h1 class="display-4"> <i class='fas fa-database'></i> Mongo Scraper</h1>
        <p class="lead">This is a simple app using Node, Express, MongoDB, and Handlebars.
            pattern</p>
        <hr class="my-4">
        <p>These articles are scraped from Rising Stack website using Cheerio.</p>
        <p>You can save articles that you like and access them later.</p>

    </div>
    <div class="row justify-content-center">
        <div class="col-md-10">
            <ul>
                {{#each articles}}
                <li>
                    <h3>{{this.title}}</h3>
                    <p>{{this.content}}</p>
                    <a href={{this.link}}>{{this.link}}</a>
                    <button id="save">Save Article</button>
                </li>
                {{/each}}
            </ul>

        </div>
    </div>
</div>